---
description: Update the installed claude-statusline to the latest release from GitHub
allowed-tools: Bash(bash:*)
---

Run exactly this and report the current/latest versions and the result:

```bash
bash "${CLAUDE_PLUGIN_ROOT}/install.sh" --update
```
